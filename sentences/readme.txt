Currently this will be where the processed sentences from the text are kept.

sentences/broad will have more sentences as the criteria will be less strict (ie: two of mission, instrument, variable)
sentences/strict will have less sentences as it will require all of mission, instrument, variable or exception